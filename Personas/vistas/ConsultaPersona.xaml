<UserControl x:Class="Personas.vistas.ConsultaPersona"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Personas.vistas"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Margin" Value="0,10,0,0"></Setter>
        </Style>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="IsReadOnly" Value="True"></Setter>
        </Style>
    </UserControl.Resources>
    <StackPanel>
        <TextBlock>Nombre</TextBlock>
        <TextBox Text="{Binding PersonaConsulta.Nombre}"></TextBox>
        <TextBlock>Edad</TextBlock>
        <TextBox Text="{Binding PersonaConsulta.Edad}"></TextBox>
        <TextBlock>Nacionalidad</TextBlock>
        <TextBox Text="{Binding PersonaConsulta.Nacionalidad}"></TextBox>
    </StackPanel>
</UserControl>
