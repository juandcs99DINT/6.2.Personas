<UserControl x:Class="Personas.vistas.NuevaPersona"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Personas.vistas"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Margin" Value="0,10,0,0"></Setter>
        </Style>
    </UserControl.Resources>
    <StackPanel>
        <TextBlock>Nombre</TextBlock>
        <TextBox></TextBox>
        <TextBlock></TextBlock>
        <TextBox></TextBox>
        <TextBlock>Nacionalidad</TextBlock>
        <StackPanel Orientation="Horizontal">
            <ComboBox Width="150" ItemsSource="{Binding ListaNacionalidades}"></ComboBox>
            <Button Width="20" Margin="5,0,0,0" Command="{Binding AñadirNacionalidadCommand}">+</Button>
        </StackPanel>
        <Button Command="{Binding AceptarCommand}" Width="150" Margin="0,5,0,0">Aceptar</Button>
    </StackPanel>
</UserControl>
